import { ReplaceAwith0Pipe } from './replace-awith0.pipe';

describe('ReplaceAwith0Pipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceAwith0Pipe();
    expect(pipe).toBeTruthy();
  });
});
